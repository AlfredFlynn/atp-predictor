{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run build && rm -rf .next && cp -R apps/web/.next .next && cp apps/web/next.config.js next.config.js"
}
